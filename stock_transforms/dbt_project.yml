name: stock_transforms
version: "1.0.0"
config-version: 2

profile: stock_transforms

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

models:
  stock_transforms:
    staging:
      +materialized: view
    intermediate:
      +materialized: table
    marts:
      +materialized: table